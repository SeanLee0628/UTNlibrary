{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "python -m pip install -r backend/requirements.txt && python -m prisma generate --schema=backend/schema.prisma"
    },
    "deploy": {
        "startCommand": "/opt/venv/bin/python main.py",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}