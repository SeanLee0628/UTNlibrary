{
  "name": "library",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd backend && py -m uvicorn main:app --reload",
    "db:deploy": "cd backend && py -m prisma migrate deploy",
    "db:push": "cd backend && py -m prisma db push"
  }
}